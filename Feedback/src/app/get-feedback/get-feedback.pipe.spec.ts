import { GetFeedbackPipe } from './get-feedback.pipe';

describe('GetFeedbackPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFeedbackPipe();
    expect(pipe).toBeTruthy();
  });
});
